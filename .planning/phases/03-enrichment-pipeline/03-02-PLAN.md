---
phase: 03-enrichment-pipeline
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/watchman/slack/actions.py
  - src/watchman/scheduler/jobs.py
  - src/watchman/main.py
  - tests/test_enrichment.py
autonomous: true
requirements: [ENRCH-01, ENRCH-02, ENRCH-03]

must_haves:
  truths:
    - "Approving a card in Slack immediately triggers enrichment"
    - "Enrichment failures do not break the Slack approval flow"
    - "A fallback enrichment job catches any cards that were missed during immediate enrichment"
    - "Tests verify enrichment trigger logic and pipeline behavior"
  artifacts:
    - path: "src/watchman/slack/actions.py"
      provides: "Enrichment trigger on card approval"
      contains: "enrich_approved_card"
    - path: "src/watchman/scheduler/jobs.py"
      provides: "Fallback enrichment scheduler job"
      contains: "run_enrichment_job"
    - path: "tests/test_enrichment.py"
      provides: "Unit tests for enrichment pipeline"
      min_lines: 50
  key_links:
    - from: "src/watchman/slack/actions.py"
      to: "src/watchman/enrichment/pipeline.py"
      via: "enrich_approved_card call on approve"
      pattern: "enrich_approved_card"
    - from: "src/watchman/scheduler/jobs.py"
      to: "src/watchman/enrichment/pipeline.py"
      via: "enrich_pending_approved in fallback job"
      pattern: "enrich_pending_approved"
---

<objective>
Wire enrichment into the Slack approval flow and scheduler, and add tests.

Purpose: Connect the enrichment pipeline to the existing approve action so enrichment triggers automatically on Gate 1 approval, with a fallback scheduler job for reliability.
Output: Modified slack/actions.py, scheduler/jobs.py, main.py; new tests/test_enrichment.py.
</objective>

<execution_context>
@/Users/salfaqih/.claude/get-shit-done/workflows/execute-plan.md
@/Users/salfaqih/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-enrichment-pipeline/03-01-SUMMARY.md
@src/watchman/slack/actions.py
@src/watchman/scheduler/jobs.py
@src/watchman/main.py
@src/watchman/enrichment/pipeline.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire enrichment trigger into Slack approve action and scheduler</name>
  <files>src/watchman/slack/actions.py, src/watchman/scheduler/jobs.py, src/watchman/main.py</files>
  <action>
Modify `src/watchman/slack/actions.py`:
1. In `_handle_review_action()`, after the DB update succeeds and card state is "approved":
   - Import `enrich_approved_card` from `watchman.enrichment.pipeline`
   - After the `client.chat_update()` call that confirms the approval, trigger enrichment:
     ```python
     if state == "approved":
         try:
             asyncio.run(enrich_approved_card(card_id, db_path))
             logger.info("Enrichment completed for card %d", card_id)
         except Exception:
             logger.exception("Enrichment failed for card %d (will retry via fallback job)", card_id)
     ```
   - The enrichment call is wrapped in try/except so failures do NOT break the approval flow (the card is already approved, the Slack message is already updated)
   - The fallback scheduler job will pick up any failed enrichments

Modify `src/watchman/scheduler/jobs.py`:
2. Add `run_enrichment_job(db_path: Path) -> None`:
   - Sync wrapper calling `asyncio.run(enrich_pending_approved(db_path))`
   - Import `enrich_pending_approved` from `watchman.enrichment.pipeline` inside the function (lazy import like scoring)
   - Log result count
   - Wrap in try/except with logger.exception on failure

3. Add `schedule_enrichment_job(scheduler: BackgroundScheduler, db_path: Path) -> None`:
   - Register enrichment job on 1-hour interval (less frequent than scoring since most enrichments happen immediately on approve)
   - Job ID: `"enrich-approved-cards"`

Modify `src/watchman/main.py`:
4. Import `schedule_enrichment_job` from `watchman.scheduler.jobs` (alongside existing imports)
5. After `schedule_scoring_job` call, add `schedule_enrichment_job(scheduler, db_path)` -- this runs regardless of Slack being enabled (catches up on approved cards from any source)
  </action>
  <verify>
    <automated>cd /Users/salfaqih/paul/Projects/watchman && python -c "
from watchman.scheduler.jobs import run_enrichment_job, schedule_enrichment_job
print('Scheduler imports OK')
import ast, inspect
source = inspect.getsource(run_enrichment_job)
assert 'enrich_pending_approved' in source, 'Missing enrich_pending_approved call'
print('Enrichment job wiring OK')

# Verify slack actions has enrichment trigger
with open('src/watchman/slack/actions.py') as f:
    content = f.read()
assert 'enrich_approved_card' in content, 'Missing enrichment trigger in actions.py'
print('Slack action wiring OK')

# Verify main.py has enrichment job
with open('src/watchman/main.py') as f:
    content = f.read()
assert 'schedule_enrichment_job' in content, 'Missing enrichment job in main.py'
print('Main.py wiring OK')
"</automated>
  </verify>
  <done>Slack approve action triggers immediate enrichment. Fallback 1-hour job catches missed enrichments. Main.py registers the fallback job.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for enrichment pipeline</name>
  <files>tests/test_enrichment.py</files>
  <action>
Create `tests/test_enrichment.py` with pytest + pytest-asyncio tests:

1. **test_build_enrichment_prompt_with_content**: Verify prompt includes card title, URL, and page content when content is provided.

2. **test_build_enrichment_prompt_fallback**: Verify prompt handles None page_content gracefully and indicates extraction from title/URL only.

3. **test_scrape_url_returns_none_on_failure**: Mock httpx to raise an exception. Verify `scrape_url()` returns None (not raises).

4. **test_scrape_url_returns_none_on_empty_extraction**: Mock httpx to return HTML but mock trafilatura.extract to return None. Verify returns None.

5. **test_enrich_card_returns_validated_entry**: Mock `anthropic.Anthropic().messages.create()` to return a valid JSON response matching IcebreakerToolEntry schema. Verify `enrich_card()` returns an IcebreakerToolEntry instance with correct fields.

6. **test_enrich_approved_card_guards_review_state**: Create a test database with a card that has review_state='pending'. Call `enrich_approved_card()`. Verify it returns None without calling the LLM (enrichment should only run for approved cards).

7. **test_enrich_approved_card_handles_scrape_failure**: Create a test database with an approved card. Mock scrape_url to return None and mock enrich_card to return a valid entry. Verify enrichment completes successfully with fallback (None page content passed to extractor).

8. **test_enrichment_state_tracking**: Create a test database with an approved card. Mock the full pipeline. Verify enrichment_state transitions from 'pending' to 'complete' and enrichment_data is stored.

Use fixtures:
- `tmp_db` fixture: creates a temp SQLite DB with `init_db()`, yields path, cleans up
- `sample_card` fixture: creates a SignalCard with review_state='approved' and inserts it

Mark tests with `@pytest.mark.unit` for pure logic tests and `@pytest.mark.integration` for database tests.
  </action>
  <verify>
    <automated>cd /Users/salfaqih/paul/Projects/watchman && python -m pytest tests/test_enrichment.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>8 tests pass covering prompt building, scraper fallback, extractor validation, pipeline guard logic, and state tracking.</done>
</task>

</tasks>

<verification>
- Approving a card in Slack triggers `enrich_approved_card()` (ENRCH-01)
- Enrichment failures do not break approval flow (graceful degradation)
- Fallback scheduler job picks up missed enrichments every hour
- Tests verify: prompt construction, scraper fallback, extractor output validation, pipeline guard on review_state, state tracking
- All 8 tests pass
</verification>

<success_criteria>
- Slack approve triggers immediate enrichment (ENRCH-01)
- Enrichment uses Claude Sonnet with json_schema output (ENRCH-02, via Plan 01)
- Validated against IcebreakerToolEntry schema (ENRCH-03, via Plan 01)
- Enrichment errors logged but do not break Slack flow
- Fallback job registered in scheduler
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-enrichment-pipeline/03-02-SUMMARY.md`
</output>
