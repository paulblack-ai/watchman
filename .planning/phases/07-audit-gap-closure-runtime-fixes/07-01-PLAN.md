---
phase: 07-audit-gap-closure-runtime-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/watchman/scheduler/jobs.py
  - src/watchman/models/raw_item.py
  - src/watchman/models/signal_card.py
  - src/watchman/storage/repositories.py
  - tests/test_collector_registry.py
  - tests/test_gate2.py
autonomous: true
requirements:
  - COLL-01
  - COLL-02
  - COLL-03
  - COLL-04

must_haves:
  truths:
    - "get_collector() resolves registered collectors at runtime (COLLECTOR_REGISTRY has 3 entries after import from watchman.collectors)"
    - "RawItem().fetched_at and SignalCard().created_at produce timezone-aware UTC datetimes"
    - "set_gate2_state('pending', ...) does NOT set gate2_reviewed_at"
    - "set_gate2_state('gate2_approved', ...) DOES set gate2_reviewed_at"
    - "All 79+ existing tests still pass after fixes"
  artifacts:
    - path: "src/watchman/scheduler/jobs.py"
      provides: "Fixed collector import triggering __init__.py side effects"
      contains: "from watchman.collectors import get_collector"
    - path: "src/watchman/models/raw_item.py"
      provides: "Timezone-aware fetched_at default"
      contains: "datetime.now(timezone.utc)"
    - path: "src/watchman/models/signal_card.py"
      provides: "Timezone-aware created_at default"
      contains: "datetime.now(timezone.utc)"
    - path: "src/watchman/storage/repositories.py"
      provides: "Conditional gate2_reviewed_at timestamp"
      contains: "if state == \"pending\""
    - path: "tests/test_collector_registry.py"
      provides: "Collector registry population and resolution tests"
      min_lines: 30
  key_links:
    - from: "src/watchman/scheduler/jobs.py"
      to: "src/watchman/collectors/__init__.py"
      via: "import from package (not base module)"
      pattern: "from watchman\\.collectors import get_collector"
    - from: "src/watchman/models/raw_item.py"
      to: "datetime.now(timezone.utc)"
      via: "Field default_factory lambda"
      pattern: "default_factory=lambda: datetime\\.now\\(timezone\\.utc\\)"
    - from: "src/watchman/storage/repositories.py"
      to: "gate2_reviewed_at SQL clause"
      via: "Python if/else branching on state=='pending'"
      pattern: "if state == .pending."
---

<objective>
Fix three critical runtime bugs found by v1.0 milestone audit: collector registry import path, naive datetime defaults, and Gate 2 timestamp logic. Add targeted test coverage for each fix.

Purpose: Without these fixes, the collection pipeline raises ValueError at runtime (empty COLLECTOR_REGISTRY), datetime comparisons raise TypeError (naive vs. aware), and Gate 2 records incorrect review timestamps on card delivery.

Output: Four production files patched, one new test file for collector registry, one new test in existing Gate 2 test file. Full test suite green.
</objective>

<execution_context>
@/Users/salfaqih/.claude/get-shit-done/workflows/execute-plan.md
@/Users/salfaqih/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-audit-gap-closure-runtime-fixes/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/watchman/collectors/__init__.py:
```python
# Already exports correctly — this is the import TARGET
from watchman.collectors import api, rss, scrape  # noqa: F401
from watchman.collectors.base import COLLECTOR_REGISTRY, get_collector
__all__ = ["COLLECTOR_REGISTRY", "get_collector"]
```

From src/watchman/collectors/base.py (registry pattern):
```python
COLLECTOR_REGISTRY: dict[str, type[BaseCollector]] = {}
def register_collector(collector_type: str): ...  # decorator
def get_collector(source: SourceConfig, db_path: Path) -> BaseCollector: ...
```

From src/watchman/models/raw_item.py line 23 (BROKEN):
```python
fetched_at: datetime = Field(default_factory=datetime.utcnow)
```

From src/watchman/models/signal_card.py line 29 (BROKEN):
```python
created_at: datetime = Field(default_factory=datetime.utcnow)
```

From src/watchman/storage/repositories.py lines 444-462 (BROKEN):
```python
async def set_gate2_state(self, card_id: int, state: str, slack_ts: str | None = None) -> None:
    await self.db.execute(
        """UPDATE cards SET gate2_state = ?, gate2_reviewed_at = datetime('now'),
           gate2_slack_ts = COALESCE(?, gate2_slack_ts) WHERE id = ?""",
        (state, slack_ts, card_id),
    )
    await self.db.commit()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix collector import, datetime defaults, and Gate 2 timestamp logic</name>
  <files>
    src/watchman/scheduler/jobs.py
    src/watchman/models/raw_item.py
    src/watchman/models/signal_card.py
    src/watchman/storage/repositories.py
  </files>
  <action>
Apply three surgical fixes to production code:

**Fix 1 — Collector import (jobs.py line 11):**
Change `from watchman.collectors.base import get_collector` to `from watchman.collectors import get_collector`. This causes `__init__.py` to execute, which imports `rss`, `api`, `scrape` modules, firing their `@register_collector` decorators and populating `COLLECTOR_REGISTRY` with all three collector types. Do NOT change any other import in the file.

**Fix 2 — Datetime awareness (raw_item.py line 23 and signal_card.py line 29):**
In `src/watchman/models/raw_item.py`:
- Change `from datetime import datetime` to `from datetime import datetime, timezone`
- Change `fetched_at: datetime = Field(default_factory=datetime.utcnow)` to `fetched_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))`

In `src/watchman/models/signal_card.py`:
- Change `from datetime import datetime` to `from datetime import datetime, timezone`
- Change `created_at: datetime = Field(default_factory=datetime.utcnow)` to `created_at: datetime = Field(default_factory=lambda: datetime.now(timezone.utc))`

Use `timezone.utc` (NOT `datetime.UTC` or `UTC` alias) to match the existing pattern in `deduplicator.py` and all three collectors.

**Fix 3 — Gate 2 timestamp conditional (repositories.py lines 444-462):**
Replace the `set_gate2_state` method body with Python if/else branching (Option A from research). When `state == "pending"`, execute SQL that sets only `gate2_state` and `gate2_slack_ts` (no `gate2_reviewed_at`). When state is anything else (`gate2_approved`, `gate2_rejected`), execute SQL that also sets `gate2_reviewed_at = datetime('now')`. Keep `await self.db.commit()` after both branches. Update the docstring to document this conditional behavior.

After all three fixes, run `cd /Users/salfaqih/paul/Projects/watchman && .venv/bin/pytest tests/ -q` to verify no existing tests break.
  </action>
  <verify>
    <automated>cd /Users/salfaqih/paul/Projects/watchman && .venv/bin/pytest tests/ -q</automated>
  </verify>
  <done>
    - jobs.py imports get_collector from watchman.collectors (not watchman.collectors.base)
    - RawItem().fetched_at.tzinfo is not None
    - SignalCard().created_at.tzinfo is not None
    - set_gate2_state has if/else branching on state == "pending"
    - All existing tests pass
  </done>
</task>

<task type="auto">
  <name>Task 2: Add test coverage for collector registry and Gate 2 timestamp fix</name>
  <files>
    tests/test_collector_registry.py
    tests/test_gate2.py
  </files>
  <action>
Create new test file and add one test to existing file:

**New file: tests/test_collector_registry.py**

Create a pytest test file with the following tests:

1. `test_collector_registry_populated` — Import `COLLECTOR_REGISTRY` from `watchman.collectors` and assert it contains exactly 3 entries: `"rss"`, `"api"`, `"scrape"`.

2. `test_get_collector_returns_rss` — Import `get_collector` from `watchman.collectors`. Create a minimal `SourceConfig` with `type="rss"`, call `get_collector(source, tmp_path / "test.db")`, and assert the returned object is an instance of `RSSCollector` (import from `watchman.collectors.rss`).

3. `test_get_collector_returns_api` — Same pattern for `type="api"`, assert instance of `APICollector` (import from `watchman.collectors.api`).

4. `test_get_collector_returns_scrape` — Same pattern for `type="scrape"`, assert instance of `ScrapeCollector` (import from `watchman.collectors.scrape`).

5. `test_raw_item_fetched_at_is_aware` — Create `RawItem(source_name="test", collector_type="rss")` and assert `item.fetched_at.tzinfo is not None`.

6. `test_signal_card_created_at_is_aware` — Create a `SignalCard` with required fields and assert `card.created_at.tzinfo is not None`.

For SourceConfig in tests 2-4, use: `SourceConfig(name="test-source", type=<type>, url="https://example.com/feed", tier=1, frequency="6h")`. Import SourceConfig from `watchman.models.source`.

**Existing file: tests/test_gate2.py**

Add one new test function `test_gate2_pending_does_not_set_reviewed_at`:
- Use the existing test pattern from `test_gate2_approve_updates_state` (same file) for DB setup
- Insert a card with enrichment data using the existing `_insert_enriched_card` helper
- Call `await repo.set_gate2_state(card_id, "pending", slack_ts="12345.67890")`
- Query the DB directly: `SELECT gate2_reviewed_at FROM cards WHERE id = ?`
- Assert the result is `None` (pending delivery must NOT set reviewed_at)
- Also assert `gate2_state` is `"pending"` and `gate2_slack_ts` is `"12345.67890"`

Run full test suite to verify all tests pass including the new ones.
  </action>
  <verify>
    <automated>cd /Users/salfaqih/paul/Projects/watchman && .venv/bin/pytest tests/test_collector_registry.py tests/test_gate2.py::test_gate2_pending_does_not_set_reviewed_at -v</automated>
  </verify>
  <done>
    - tests/test_collector_registry.py exists with 6 tests, all passing
    - tests/test_gate2.py has new test_gate2_pending_does_not_set_reviewed_at, passing
    - Full test suite green: .venv/bin/pytest tests/ -q shows 0 failures
  </done>
</task>

</tasks>

<verification>
1. Collector registry: `python -c "from watchman.collectors import COLLECTOR_REGISTRY; assert len(COLLECTOR_REGISTRY) == 3; print('OK:', list(COLLECTOR_REGISTRY.keys()))"` (run from project root with PYTHONPATH=src)
2. Datetime awareness: `python -c "from watchman.models.raw_item import RawItem; from watchman.models.signal_card import SignalCard; r = RawItem(source_name='t', collector_type='rss'); s = SignalCard(title='t', source_name='t', date='2026-01-01', url='https://x.com', tier=1, collector_type='rss', url_hash='a'); assert r.fetched_at.tzinfo is not None; assert s.created_at.tzinfo is not None; print('OK: both aware')"` (PYTHONPATH=src)
3. Gate 2 timestamp: Verified by test_gate2_pending_does_not_set_reviewed_at
4. Full regression: `.venv/bin/pytest tests/ -v --tb=short` — all tests pass, 0 failures
</verification>

<success_criteria>
- COLLECTOR_REGISTRY contains 3 entries (rss, api, scrape) when imported from watchman.collectors
- RawItem().fetched_at and SignalCard().created_at are timezone-aware (tzinfo is not None)
- set_gate2_state("pending", ...) leaves gate2_reviewed_at as NULL
- set_gate2_state("gate2_approved", ...) sets gate2_reviewed_at to current time
- Full test suite passes with 0 failures (85+ tests including new ones)
</success_criteria>

<output>
After completion, create `.planning/phases/07-audit-gap-closure-runtime-fixes/07-01-SUMMARY.md`
</output>
