---
phase: human-verification
task: 7
total_tasks: 7
status: complete
last_updated: 2026-02-25T22:35:00.000Z
---

<current_state>
HUMAN VERIFICATION COMPLETE — all 7 tests passed. v1.0 milestone is fully verified.

Scoring completed: 1,666/1,667 cards scored (avg 6.45). Daily delivery, button actions, enrichment, Gate 2, add-source, and snooze re-queue all verified live in Slack.

Four bugs found and fixed during verification:
1. OpenRouter base URL double /v1/ (SDK v0.83.0 compatibility)
2. Scoring prompt causing ~70% JSON parse failures (now explicit schema)
3. Sequential scoring taking 3hrs (now parallel batches of 20, ~15min)
4. asyncio.run() inside running event loop in Gate 2 delivery
</current_state>

<completed_work>

- Phase 7: Plan + Execute + Verify — DONE
- Milestone audit: DONE — 27/27 requirements
- Human Test 1 (Collection): PASSED — 17 sources, 1,669 raw items, all 3 collector types
- Human Test 2 (Health Alert): PASSED — Slack DM on consecutive zeros
- Human Test 3 (Daily Delivery): PASSED — 5 top-scored cards delivered as Block Kit cards
- Human Test 4 (Buttons): PASSED — Approve/Reject/Snooze all update DB state correctly
- Human Test 5 (Add Source): PASSED — /watchman add-source added Crescendo (18 sources total)
- Human Test 6 (Snooze Re-queue): PASSED — expired snooze re-appears in delivery
- Human Test 7 (Enrichment + Gate 2): PASSED — 3 cards enriched, Gate 2 cards posted to Slack
- Scoring: 1,666/1,667 scored via parallel batches (avg score 6.45)
- All fixes committed: 4051494
</completed_work>

<remaining_work>

- `/gsd:complete-milestone v1.0`
</remaining_work>

<decisions_made>

- Fixed OpenRouter model IDs: `anthropic/claude-haiku-4.5` (not `claude-haiku-4-5-20251001`) and `anthropic/claude-sonnet-4` (not `claude-sonnet-4-20250514`)
- Removed `betas` and `output_config` params — OpenRouter doesn't support Anthropic-native structured output; using plain text response with JSON parsing + markdown fence stripping instead
- User fixed SLACK_PAUL_USER_ID from D-prefix (DM channel) to U-prefix (user ID) for DM delivery
- Watchman process should be started fresh each session: `source .env && export SLACK_BOT_TOKEN SLACK_APP_TOKEN SLACK_CHANNEL_ID SLACK_PAUL_USER_ID OPENROUTER_API_KEY WATCHMAN_DB_PATH && nohup .venv/bin/python -c "from watchman.main import main; main()" > /tmp/watchman.log 2>&1 &`
</decisions_made>

<blockers>
- 3 source URLs are dead (Anthropic Blog RSS 404, Meta AI Blog RSS 404, Product Hunt AI 403) — not bugs, just stale URLs in sources.yaml. Non-blocking for verification.
- Scoring needs to run before delivery can be tested (cards must be scored to appear in daily review)
</blockers>

<context>
The user wants to do live Slack verification — "Start the app, I'll test in Slack" approach. The workflow is: start Watchman app -> run scoring manually -> trigger delivery manually -> user clicks buttons in Slack -> we check DB state after each action.

For scoring, use: `source .env && export vars && .venv/bin/python -c "import asyncio; from pathlib import Path; from watchman.scoring.scorer import score_unscored_cards; print(asyncio.run(score_unscored_cards(Path('watchman.db'), Path('src/watchman/config/rubric.yaml'))))"`

For delivery, use: `source .env && export vars && .venv/bin/python -c "import asyncio; from pathlib import Path; from watchman.slack.delivery import deliver_daily_review; print(asyncio.run(deliver_daily_review(Path('watchman.db'), Path('src/watchman/config/rubric.yaml'))))"`

The app needs to be running for Slack button interactions to work (Socket Mode listener).
</context>

<next_action>
1. Start Watchman app (background process with Socket Mode for button handling)
2. Run scoring manually: score_unscored_cards() — will score 1,667 cards via Claude Haiku (may take several minutes and cost ~$1-2 in API calls)
3. Trigger deliver_daily_review() to send top 5 scored cards to Slack
4. Have user click Approve on one card, verify enrichment triggers
5. Continue through remaining button tests
</next_action>
