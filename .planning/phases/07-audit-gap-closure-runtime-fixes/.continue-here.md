---
phase: human-verification
task: 3
total_tasks: 8
status: in_progress
last_updated: 2026-02-25T19:21:17.838Z
---

<current_state>
We are doing HUMAN VERIFICATION of the entire v1.0 milestone. Phase 7 code is complete and all 86 tests pass. The milestone audit passed (27/27 requirements). We are now running through the 9 "human_needed" verification items that require a live Slack workspace — Phases 1, 2, and 3 had items that couldn't be verified without live Slack interaction.

We completed Phase 1 verification (collection + health alerts) and discovered/fixed an OpenRouter compatibility bug in scorer.py and extractor.py (wrong model IDs, unsupported `betas` and `output_config` params). That fix is committed.

The DB has 1,669 raw items from 13/17 sources (3 sources have dead URLs — Anthropic Blog 404, Meta AI Blog 404, Product Hunt 403). Normalization produced 1,667 signal cards. Scoring has NOT yet run successfully (was blocked by the OpenRouter bug, now fixed).
</current_state>

<completed_work>

- Phase 7: Plan + Execute + Verify — DONE (all code changes committed)
- Milestone audit: DONE — 27/27 requirements, report at .planning/v1.0-MILESTONE-AUDIT.md
- REQUIREMENTS.md: COLL-01 through COLL-04 checkboxes updated to [x]
- ROADMAP.md: Phase 5 and 7 checkboxes and progress table updated
- Human Test 1 (Phase 1 — Collection): PASSED — 17 sources configured, 14 returned items (1,669 raw items), all 3 collector types work (RSS/API/scrape)
- Human Test 2 (Phase 1 — Health Alert): PASSED — Slack DM sent on 2+ consecutive zeros (had to fix SLACK_PAUL_USER_ID from D-prefix to U-prefix)
- OpenRouter fix: Committed — scorer uses `anthropic/claude-haiku-4.5`, extractor uses `anthropic/claude-sonnet-4`, removed `betas` and `output_config` params
- DB state: 1,669 raw items, 1,667 signal cards (normalized), 0 scored (scoring not yet run with fixed code)
</completed_work>

<remaining_work>

- Human Test 3 (Phase 2 — Daily Delivery): Run scoring first (now fixed), then trigger `deliver_daily_review()` — verify Block Kit cards appear in Slack
- Human Test 4 (Phase 2 — Buttons): Click Approve, Reject, Snooze, Details on delivered cards — verify state updates and message changes
- Human Test 5 (Phase 2 — /watchman add-source): Run slash command in Slack — verify source added to YAML
- Human Test 6 (Phase 2 — Snooze re-queue): Insert expired snoozed card, run delivery, verify it re-appears
- Human Test 7 (Phase 3 — Enrichment on approve): Approve a card, verify enrichment runs and Gate 2 card appears
- Update Phase 1/2/3 VERIFICATION.md files from `human_needed` to `passed`
- Update milestone audit to reflect all verifications passed
- Then: `/gsd:complete-milestone v1.0`
</remaining_work>

<decisions_made>

- Fixed OpenRouter model IDs: `anthropic/claude-haiku-4.5` (not `claude-haiku-4-5-20251001`) and `anthropic/claude-sonnet-4` (not `claude-sonnet-4-20250514`)
- Removed `betas` and `output_config` params — OpenRouter doesn't support Anthropic-native structured output; using plain text response with JSON parsing + markdown fence stripping instead
- User fixed SLACK_PAUL_USER_ID from D-prefix (DM channel) to U-prefix (user ID) for DM delivery
- Watchman process should be started fresh each session: `source .env && export SLACK_BOT_TOKEN SLACK_APP_TOKEN SLACK_CHANNEL_ID SLACK_PAUL_USER_ID OPENROUTER_API_KEY WATCHMAN_DB_PATH && nohup .venv/bin/python -c "from watchman.main import main; main()" > /tmp/watchman.log 2>&1 &`
</decisions_made>

<blockers>
- 3 source URLs are dead (Anthropic Blog RSS 404, Meta AI Blog RSS 404, Product Hunt AI 403) — not bugs, just stale URLs in sources.yaml. Non-blocking for verification.
- Scoring needs to run before delivery can be tested (cards must be scored to appear in daily review)
</blockers>

<context>
The user wants to do live Slack verification — "Start the app, I'll test in Slack" approach. The workflow is: start Watchman app -> run scoring manually -> trigger delivery manually -> user clicks buttons in Slack -> we check DB state after each action.

For scoring, use: `source .env && export vars && .venv/bin/python -c "import asyncio; from pathlib import Path; from watchman.scoring.scorer import score_unscored_cards; print(asyncio.run(score_unscored_cards(Path('watchman.db'), Path('src/watchman/config/rubric.yaml'))))"`

For delivery, use: `source .env && export vars && .venv/bin/python -c "import asyncio; from pathlib import Path; from watchman.slack.delivery import deliver_daily_review; print(asyncio.run(deliver_daily_review(Path('watchman.db'), Path('src/watchman/config/rubric.yaml'))))"`

The app needs to be running for Slack button interactions to work (Socket Mode listener).
</context>

<next_action>
1. Start Watchman app (background process with Socket Mode for button handling)
2. Run scoring manually: score_unscored_cards() — will score 1,667 cards via Claude Haiku (may take several minutes and cost ~$1-2 in API calls)
3. Trigger deliver_daily_review() to send top 5 scored cards to Slack
4. Have user click Approve on one card, verify enrichment triggers
5. Continue through remaining button tests
</next_action>
