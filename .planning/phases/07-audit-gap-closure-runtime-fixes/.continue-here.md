---
phase: aws-deployment-and-source-fixes
task: 3
total_tasks: 5
status: in_progress
last_updated: 2026-03-01T12:25:10.710Z
---

<current_state>
Watchman is deployed and running on AWS EC2 (i-0d34932e5ba004c4b, 54.234.12.180) as a systemd service.
Quick task 6 completed: added JinaCollector and fixed 8 broken sources. 227 cards scored, 5 delivered to Slack.
Three tasks remain: fix Meta junk cards, add Lauren's new sources, build YouTube collector.
</current_state>

<completed_work>

- Deployed Watchman to AWS EC2 t3.micro (i-0d34932e5ba004c4b, IP: 54.234.12.180)
  - SSH: `ssh -i ~/.ssh/gathering-agent-key.pem ec2-user@54.234.12.180`
  - systemd service: `sudo systemctl {status|restart|stop} watchman`
  - Logs: `sudo journalctl -u watchman --no-pager -n 50`
  - Venv: ~/watchman/venv, PYTHONPATH=src
  - Instance renamed from "gathering-agent-taxonomy-flex" to "watchman"
- Fixed `max_age_days=14` in scheduler/jobs.py (prevents old cards)
- Fixed `requires-python` from >=3.11 to >=3.9 in pyproject.toml
- Created JinaCollector (`src/watchman/collectors/jina.py`) using r.jina.ai markdown API
- Updated RawItem and SignalCard models to accept "jina" collector_type
- Fixed 8 broken source URLs (quick task 6, commit f1d240a)
- Manual collection run: 260 raw items, 227 signal cards, 5 delivered to Slack
- Committed SignalCard fix (3b9cdfa)

</completed_work>

<remaining_work>

- Task 1: Fix Meta AI Blog junk cards
  - JinaCollector parses markdown image refs as entries: `![Image 17`, `![Image 18`
  - URLs point to Facebook CDN (fbcdn.net) image files, not blog posts
  - Fix: filter entries in jina.py where title starts with `![` or URL contains image extensions/fbcdn
  - Also: DELETE junk cards from EC2 DB (WHERE source_name='Meta AI Blog' AND title LIKE '![Image%')

- Task 2: Add Lauren's new sources to sources.yaml (all as type: jina)
  - theresanaiforthat.com (AI tool directory)
  - futuretools.io (AI tool directory)
  - tools.forwardfuture.ai (AI tool directory, 700+ tools)
  - aitoolsdirectory.com (AI tool directory)
  - aixploria.com/en/ultimate-list-ai/ (AI tool list)
  - nxgntools.com (AI tool directory + launch platform)
  - TechCrunch AI - ALREADY EXISTS as RSS, skip
  - Decide tier (probably Tier 2, 12h frequency)

- Task 3: Build YouTube channel collector
  - Channels: @futurepedia_io (Futurepedia), @mreflow (Matt Wolfe)
  - Approach: YouTube RSS feeds for new video detection
    - Feed URL format: https://www.youtube.com/feeds/videos.xml?channel_id=CHANNEL_ID
    - Need to resolve @handle to channel_id
  - pip install youtube-transcript-api for transcriptions
  - Light LLM scan (Claude Haiku) on transcript to extract AI tool/feature mentions
  - Create cards only for videos mentioning relevant tools
  - New file: src/watchman/collectors/youtube.py
  - Add "youtube" to collector_type Literals in RawItem and SignalCard
  - Add youtube-transcript-api to pyproject.toml dependencies

</remaining_work>

<decisions_made>

- EC2 t3.micro with systemd service (simplest for long-running process with Socket Mode)
- Reused existing stopped instance (gathering-agent-taxonomy-flex) instead of new one
- JinaCollector uses r.jina.ai to get markdown, parses headings for changelogs and links for blog listings
- max_age_days=14 hardcoded in collect_source() to prevent old cards
- Anthropic and Meta have no public RSS feeds — use Jina-based collection from their blog pages
- After local code changes, sync to EC2 via rsync/scp then restart systemd service

</decisions_made>

<blockers>
- VentureBeat AI RSS returned 0 items in last collection run (was 7 before) — intermittent, monitor
- YouTube channel_ids need to be resolved from @handles before adding to sources.yaml
- youtube-transcript-api needs to be added as a dependency and installed on EC2
</blockers>

<context>
Lauren provided a list of AI tool directories and YouTube channels to add as sources.
She asked about parsing YouTube transcriptions for tool mentions — confirmed this is feasible
using youtube-transcript-api + light LLM judge (Claude Haiku via OpenRouter).

The Meta AI Blog junk cards are the most urgent fix — they're polluting the daily delivery.
Filter should be in jina.py's parsing logic to skip image-only markdown entries.

Deployment workflow: edit locally -> sync to EC2 -> restart service.
Full sync command: rsync -avz --include='*.py' --include='*.yaml' --include='*/' --exclude='*' -e "ssh -o ServerAliveInterval=15 -i ~/.ssh/gathering-agent-key.pem" /Users/salfaqih/paul/Projects/watchman/src/watchman/ ec2-user@54.234.12.180:~/watchman/src/watchman/
</context>

<next_action>
Start with: Fix jina.py to filter out image markdown entries (title starts with "![" or URL contains fbcdn/image extensions), then clean junk Meta cards from EC2 DB, then add Lauren's sources, then build YouTube collector.
</next_action>
