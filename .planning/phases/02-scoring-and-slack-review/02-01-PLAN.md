---
phase: 02-scoring-and-slack-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/watchman/scoring/__init__.py
  - src/watchman/scoring/scorer.py
  - src/watchman/scoring/rubric.py
  - src/watchman/scoring/models.py
  - src/watchman/config/rubric.yaml
  - src/watchman/storage/database.py
  - src/watchman/storage/repositories.py
  - src/watchman/models/signal_card.py
  - src/watchman/scheduler/jobs.py
  - tests/test_scoring.py
autonomous: true
requirements: [PROC-04, PROC-05, PROC-06]

must_haves:
  truths:
    - "New signal cards are scored against a 4-dimension relevance rubric via Claude Haiku"
    - "Scores include composite score, per-dimension breakdown, and top contributing dimension"
    - "Rubric weights are loaded from YAML config and can be changed without code edits"
    - "Score breakdowns are persisted in the database per signal card"
    - "Daily cap logic selects top N cards (3-7) by composite score, with tier as tiebreaker"
  artifacts:
    - path: "src/watchman/scoring/scorer.py"
      provides: "Claude Haiku scoring with structured outputs"
      exports: ["score_card"]
    - path: "src/watchman/scoring/rubric.py"
      provides: "Rubric YAML config loader"
      exports: ["load_rubric", "RubricConfig"]
    - path: "src/watchman/scoring/models.py"
      provides: "Pydantic models for score data"
      exports: ["DimensionScore", "RubricScore"]
    - path: "src/watchman/config/rubric.yaml"
      provides: "Default rubric weights configuration"
      contains: "taxonomy_fit"
    - path: "src/watchman/storage/database.py"
      provides: "Phase 2 schema migration (score + review columns)"
      contains: "relevance_score"
    - path: "src/watchman/storage/repositories.py"
      provides: "CardRepository scoring and review query methods"
      exports: ["CardRepository"]
  key_links:
    - from: "src/watchman/scoring/scorer.py"
      to: "anthropic API"
      via: "client.messages.create with output_config.format"
      pattern: "output_config.*json_schema"
    - from: "src/watchman/scoring/scorer.py"
      to: "src/watchman/scoring/rubric.py"
      via: "RubricConfig weights used in scoring prompt"
      pattern: "RubricConfig"
    - from: "src/watchman/scheduler/jobs.py"
      to: "src/watchman/scoring/scorer.py"
      via: "score_card called after normalization"
      pattern: "score_card"
---

<objective>
Implement the LLM scoring engine using Claude Haiku structured outputs, rubric YAML configuration, database schema migration for score/review columns, and daily volume cap selection logic.

Purpose: Signal cards need relevance scores before they can be presented to Lauren. This plan builds the scoring pipeline step that runs after normalization, persists score breakdowns, and identifies the top N cards for daily review.

Output: Scoring module, rubric config, updated database schema, and CardRepository methods for scored card queries.
</objective>

<execution_context>
@/Users/salfaqih/.claude/get-shit-done/workflows/execute-plan.md
@/Users/salfaqih/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-scoring-and-slack-review/02-CONTEXT.md
@.planning/phases/02-scoring-and-slack-review/02-RESEARCH.md

@src/watchman/models/signal_card.py
@src/watchman/storage/database.py
@src/watchman/storage/repositories.py
@src/watchman/scheduler/jobs.py
@src/watchman/config/loader.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scoring models, rubric config, and database migration</name>
  <files>
    src/watchman/scoring/__init__.py
    src/watchman/scoring/models.py
    src/watchman/scoring/rubric.py
    src/watchman/config/rubric.yaml
    src/watchman/storage/database.py
    src/watchman/storage/repositories.py
    src/watchman/models/signal_card.py
  </files>
  <action>
    1. Create `src/watchman/scoring/__init__.py` (empty).

    2. Create `src/watchman/scoring/models.py` with Pydantic v2 models:
       - `DimensionScore(BaseModel)`: `score: float` (0.0-10.0), `rationale: str`
       - `RubricScore(BaseModel)`: `taxonomy_fit: DimensionScore`, `novel_capability: DimensionScore`, `adoption_traction: DimensionScore`, `credibility: DimensionScore`, `composite_score: float`, `top_dimension: str`

    3. Create `src/watchman/scoring/rubric.py`:
       - `RubricDimension(BaseModel)`: `weight: float`, `description: str`
       - `RubricConfig(BaseModel)`: `dimensions: dict[str, RubricDimension]`, `score_scale: int = 10`, `daily_cap_min: int = 3`, `daily_cap_max: int = 7`, `daily_cap_target: int = 5`
       - `load_rubric(path: Path) -> RubricConfig`: loads YAML with `yaml.safe_load`, validates with Pydantic (follow existing `loader.py` pattern)

    4. Create `src/watchman/config/rubric.yaml` with default weights per user decision:
       - taxonomy_fit: weight 0.35, description about IcebreakerAI taxonomy fit
       - novel_capability: weight 0.30, description about new capabilities
       - adoption_traction: weight 0.20, description about adoption evidence
       - credibility: weight 0.15, description about source credibility
       - score_scale: 10, daily_cap_min: 3, daily_cap_max: 7, daily_cap_target: 5

    5. Update `src/watchman/storage/database.py` — add a `migrate_phase2(db_path)` async function that runs ALTER TABLE statements to add columns to `cards`:
       - `relevance_score REAL`
       - `score_breakdown TEXT` (JSON string)
       - `top_dimension TEXT`
       - `review_state TEXT DEFAULT 'pending'` (CHECK constraint: pending/approved/rejected/snoozed)
       - `reviewed_at TEXT`
       - `snooze_until TEXT`
       - `slack_message_ts TEXT`
       - `slack_channel_id TEXT`
       - Add indexes: `idx_cards_review_state` on `review_state`, `idx_cards_snooze_until` on `snooze_until`
       - Use try/except around each ALTER TABLE to handle "duplicate column name" gracefully (idempotent migration)
       - Call `migrate_phase2` from `init_db` at the end (after table creation)

    6. Update `src/watchman/storage/repositories.py` — add methods to `CardRepository`:
       - `async def save_score(self, card_id: int, score: RubricScore) -> None`: updates `relevance_score`, `score_breakdown` (JSON via `score.model_dump_json()`), `top_dimension` on the card row
       - `async def find_unscored(self) -> list[SignalCard]`: selects cards where `relevance_score IS NULL AND duplicate_of IS NULL`
       - `async def find_top_scored_today(self, limit: int) -> list[SignalCard]`: selects cards where `review_state = 'pending'` and `relevance_score IS NOT NULL` and created today or snoozed cards with `snooze_until <= datetime('now')`, ordered by `relevance_score DESC, tier ASC`, limit N
       - `async def count_scored_today(self) -> int`: counts all scored cards from today (for "Showing X of Y" footer)
       - `async def set_review_state(self, card_id: int, state: str, slack_ts: str | None = None, slack_channel: str | None = None) -> None`: updates `review_state`, `reviewed_at`, and optionally `slack_message_ts`/`slack_channel_id`
       - `async def snooze_card(self, card_id: int, days: int = 30) -> None`: sets `review_state='snoozed'`, `snooze_until` to now + days
       - Update `_row_to_card` to handle the new optional columns (default None if column missing or NULL)

    7. Update `src/watchman/models/signal_card.py` — add optional fields to `SignalCard`:
       - `relevance_score: float | None = None`
       - `score_breakdown: str | None = None` (JSON string)
       - `top_dimension: str | None = None`
       - `review_state: str = "pending"`
       - `reviewed_at: datetime | None = None`
       - `snooze_until: datetime | None = None`
       - `slack_message_ts: str | None = None`
       - `slack_channel_id: str | None = None`
  </action>
  <verify>
    <automated>cd /Users/salfaqih/paul/Projects/watchman && python -c "
from watchman.scoring.models import DimensionScore, RubricScore
from watchman.scoring.rubric import load_rubric
from pathlib import Path
rc = load_rubric(Path('src/watchman/config/rubric.yaml'))
assert len(rc.dimensions) == 4
assert abs(sum(d.weight for d in rc.dimensions.values()) - 1.0) < 0.01
print('Rubric OK:', list(rc.dimensions.keys()))
import asyncio
from watchman.storage.database import init_db
asyncio.run(init_db(Path('/tmp/test_phase2.db')))
import aiosqlite, sqlite3
async def check():
    async with aiosqlite.connect('/tmp/test_phase2.db') as db:
        db.row_factory = sqlite3.Row
        cursor = await db.execute('PRAGMA table_info(cards)')
        cols = [r[1] for r in await cursor.fetchall()]
        assert 'relevance_score' in cols, f'Missing relevance_score in {cols}'
        assert 'review_state' in cols, f'Missing review_state in {cols}'
        assert 'snooze_until' in cols, f'Missing snooze_until in {cols}'
        print('DB columns OK:', [c for c in cols if c not in ('id','title','source_name','date','url','tier','summary','collector_type','url_hash','content_fingerprint','duplicate_of','seen_count','created_at')])
asyncio.run(check())
print('ALL CHECKS PASSED')
"
    </automated>
    <manual>Verify rubric.yaml has all 4 dimensions with weights summing to 1.0</manual>
  </verify>
  <done>
    - RubricScore and DimensionScore Pydantic models validate correctly
    - rubric.yaml loads with 4 dimensions, weights sum to 1.0
    - Database migration adds all 8 new columns to cards table
    - CardRepository has save_score, find_unscored, find_top_scored_today, set_review_state, snooze_card methods
    - SignalCard model includes all new optional fields
  </done>
</task>

<task type="auto">
  <name>Task 2: Claude Haiku scorer and scheduler integration</name>
  <files>
    src/watchman/scoring/scorer.py
    src/watchman/scheduler/jobs.py
    tests/test_scoring.py
  </files>
  <action>
    1. Create `src/watchman/scoring/scorer.py`:
       - `async def score_card(card: SignalCard, rubric: RubricConfig) -> RubricScore`:
         - Builds a scoring prompt that includes: the card's title, URL, summary, source name, tier, and the rubric dimension descriptions with weights
         - Prompt instructs Claude to score each dimension 0-10 with rationale, compute weighted composite score, and identify the top contributing dimension
         - Calls `anthropic.Anthropic().messages.create()` with:
           - `model="claude-haiku-4-5-20251001"`
           - `max_tokens=512`
           - `output_config={"format": {"type": "json_schema", "schema": RubricScore.model_json_schema()}}`
         - Parses response with `RubricScore.model_validate_json(response.content[0].text)`
         - Returns the RubricScore
       - `async def score_unscored_cards(db_path: Path, rubric_path: Path) -> int`:
         - Opens DB connection, loads rubric, finds unscored non-duplicate cards
         - Scores each card sequentially (not parallel — rate limit friendly)
         - Saves score to DB via `CardRepository.save_score()`
         - Returns count of newly scored cards
         - Uses logging (not print) for progress

    2. Update `src/watchman/scheduler/jobs.py`:
       - Import `score_unscored_cards` from `watchman.scoring.scorer`
       - In `collect_source()`, after `record_collection_result`, call `asyncio.run(score_unscored_cards(db_path, rubric_path))` to score any new cards
       - Update `setup_scheduler` to accept `rubric_path: Path` parameter and pass it through
       - Alternatively (simpler): add a dedicated scoring job that runs every 30 minutes via IntervalTrigger, calling `score_unscored_cards`. This is cleaner than coupling to each collector job.
       - Use the dedicated job approach: add `schedule_scoring_job(scheduler, db_path, rubric_path)` that adds a 30-min interval job

    3. Create `tests/test_scoring.py`:
       - Test `load_rubric` returns valid config with 4 dimensions
       - Test `RubricScore` model validates correctly with sample data
       - Test `DimensionScore` rejects score outside 0-10 range (if validation added) or accepts valid scores
       - Test composite score calculation concept (weights * scores)
       - Mock the Anthropic client to test `score_card` returns a valid RubricScore
       - Use `pytest.mark.unit` for pure logic tests, `pytest.mark.integration` for mock-API tests
  </action>
  <verify>
    <automated>cd /Users/salfaqih/paul/Projects/watchman && python -m pytest tests/test_scoring.py -x -v 2>&1 | tail -20</automated>
    <manual>Verify scorer.py uses claude-haiku-4-5-20251001 model and output_config.format structured outputs</manual>
  </verify>
  <done>
    - scorer.py calls Claude Haiku with structured outputs and returns RubricScore
    - Scoring prompt includes rubric dimensions, weights, and card details
    - Scheduler has a dedicated 30-min scoring job
    - Tests pass for rubric loading, model validation, and mocked scoring
  </done>
</task>

</tasks>

<verification>
1. `python -c "from watchman.scoring.scorer import score_card; print('scorer imports OK')"` succeeds
2. `python -c "from watchman.scoring.rubric import load_rubric; print('rubric imports OK')"` succeeds
3. `python -m pytest tests/test_scoring.py -x` passes
4. Database migration is idempotent (can run init_db twice without error)
5. Rubric YAML has 4 dimensions with weights summing to 1.0
</verification>

<success_criteria>
- Claude Haiku structured output scoring pipeline is implemented and testable
- Rubric weights are configurable via YAML (not hardcoded) per user decision
- Score breakdowns (composite + per-dimension + top dimension) are persisted per card per user decision
- CardRepository provides methods for daily cap selection (top N by score, tier tiebreaker)
- All tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-scoring-and-slack-review/02-01-SUMMARY.md`
</output>
