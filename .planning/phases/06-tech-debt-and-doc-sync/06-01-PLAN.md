---
phase: 06-tech-debt-and-doc-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/watchman/collectors/rss.py
  - src/watchman/collectors/api.py
  - src/watchman/collectors/scrape.py
  - src/watchman/processing/deduplicator.py
  - src/watchman/storage/repositories.py
  - src/watchman/enrichment/extractor.py
  - tests/test_enrichment.py
  - .planning/phases/02-scoring-and-slack-review/02-01-SUMMARY.md
  - .planning/phases/03-enrichment-pipeline/03-01-SUMMARY.md
  - .planning/phases/03-enrichment-pipeline/03-02-SUMMARY.md
  - .planning/REQUIREMENTS.md
  - .planning/phases/01-collection-pipeline/01-VERIFICATION.md
  - .planning/phases/03-enrichment-pipeline/03-VERIFICATION.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "No datetime.utcnow() calls remain in src/ or tests/ directories"
    - "All SUMMARY docs reference OPENROUTER_API_KEY instead of ANTHROPIC_API_KEY"
    - "REQUIREMENTS.md checkboxes for SLCK-01 through SLCK-04 and OUT-01 through OUT-03 are checked"
    - "VERIFICATION.md files exist for Phase 1 and Phase 3"
  artifacts:
    - path: ".planning/phases/01-collection-pipeline/01-VERIFICATION.md"
      provides: "Phase 1 verification report"
    - path: ".planning/phases/03-enrichment-pipeline/03-VERIFICATION.md"
      provides: "Phase 3 verification report"
  key_links:
    - from: "src/watchman/collectors/*.py"
      to: "datetime.now(timezone.utc)"
      via: "import from datetime"
      pattern: "datetime\\.now\\(timezone\\.utc\\)"
---

<objective>
Fix deprecated datetime.utcnow() calls across the codebase, sync SUMMARY docs to reference OPENROUTER_API_KEY, update REQUIREMENTS.md checkboxes to match actual implementation status, and create missing VERIFICATION.md files for Phases 1 and 3.

Purpose: Close tech debt items from v1 audit — no behavioral changes, pure maintenance
Output: Clean codebase with no deprecated API warnings, accurate documentation, complete verification artifacts
</objective>

<execution_context>
@/Users/salfaqih/.claude/get-shit-done/workflows/execute-plan.md
@/Users/salfaqih/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-tech-debt-and-doc-sync/06-RESEARCH.md
@.planning/phases/02-scoring-and-slack-review/02-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace deprecated datetime.utcnow() and sync documentation</name>
  <files>
    src/watchman/collectors/rss.py
    src/watchman/collectors/api.py
    src/watchman/collectors/scrape.py
    src/watchman/processing/deduplicator.py
    src/watchman/storage/repositories.py
    src/watchman/enrichment/extractor.py
    tests/test_enrichment.py
    .planning/phases/02-scoring-and-slack-review/02-01-SUMMARY.md
    .planning/phases/03-enrichment-pipeline/03-01-SUMMARY.md
    .planning/phases/03-enrichment-pipeline/03-02-SUMMARY.md
    .planning/REQUIREMENTS.md
  </files>
  <action>
  **datetime.utcnow() replacement (6 source files + 1 test file):**

  In each file, add `timezone` to the existing `from datetime import ...` line, then replace `datetime.utcnow()` with `datetime.now(timezone.utc)`:

  1. `src/watchman/collectors/rss.py` line 51: `now = datetime.utcnow()` -> `now = datetime.now(timezone.utc)`
  2. `src/watchman/collectors/api.py` line 38: `now = datetime.utcnow()` -> `now = datetime.now(timezone.utc)`
  3. `src/watchman/collectors/scrape.py` line 32: `now = datetime.utcnow()` -> `now = datetime.now(timezone.utc)`
  4. `src/watchman/processing/deduplicator.py` line 63: `cutoff = datetime.utcnow() - timedelta(...)` -> `cutoff = datetime.now(timezone.utc) - timedelta(...)`
  5. `src/watchman/storage/repositories.py` line 370: `snooze_until = (datetime.utcnow() + timedelta(...))` -> `snooze_until = (datetime.now(timezone.utc) + timedelta(...))`
  6. `src/watchman/enrichment/extractor.py` line 118: `discovered_at=datetime.utcnow()` -> `discovered_at=datetime.now(timezone.utc)`
  7. `tests/test_enrichment.py` lines 203 and 235: `discovered_at=datetime.utcnow()` -> `discovered_at=datetime.now(timezone.utc)`

  For each file, ensure `timezone` is in the import. Example: `from datetime import datetime, timedelta` becomes `from datetime import datetime, timedelta, timezone`.

  **SUMMARY doc API key references (3 files):**

  1. `.planning/phases/02-scoring-and-slack-review/02-01-SUMMARY.md`: Replace all occurrences of `ANTHROPIC_API_KEY` with `OPENROUTER_API_KEY`
  2. `.planning/phases/03-enrichment-pipeline/03-01-SUMMARY.md`: Replace all occurrences of `ANTHROPIC_API_KEY` with `OPENROUTER_API_KEY`
  3. `.planning/phases/03-enrichment-pipeline/03-02-SUMMARY.md`: Replace all occurrences of `ANTHROPIC_API_KEY` with `OPENROUTER_API_KEY`

  **REQUIREMENTS.md checkbox updates:**

  In `.planning/REQUIREMENTS.md`, change unchecked boxes to checked for:
  - `[ ] **SLCK-01**` -> `[x] **SLCK-01**`
  - `[ ] **SLCK-02**` -> `[x] **SLCK-02**`
  - `[ ] **SLCK-03**` -> `[x] **SLCK-03**`
  - `[ ] **SLCK-04**` -> `[x] **SLCK-04**`
  - `[ ] **OUT-01**` -> `[x] **OUT-01**`
  - `[ ] **OUT-02**` -> `[x] **OUT-02**`
  - `[ ] **OUT-03**` -> `[x] **OUT-03**`

  Also update the Traceability table:
  - SLCK-01 through SLCK-04: Status from `Pending` to `Complete`
  - OUT-01 through OUT-03: Already show Complete in traceability, verify they match

  Also check Phase 5 requirements (PROC-01, PROC-02, PROC-03, SRC-04) — if Phase 5 is complete, update those checkboxes and traceability status too.
  </action>
  <verify>
    <automated>cd /Users/salfaqih/paul/Projects/watchman && grep -r "datetime\.utcnow()" src/ tests/ --include="*.py" | grep -v __pycache__ | wc -l | xargs test 0 -eq && echo "PASS: No utcnow calls" || echo "FAIL: utcnow calls remain"</automated>
    <automated>cd /Users/salfaqih/paul/Projects/watchman && grep -r "ANTHROPIC_API_KEY" .planning/phases/02-scoring-and-slack-review/02-01-SUMMARY.md .planning/phases/03-enrichment-pipeline/03-01-SUMMARY.md .planning/phases/03-enrichment-pipeline/03-02-SUMMARY.md | wc -l | xargs test 0 -eq && echo "PASS: No ANTHROPIC refs" || echo "FAIL: ANTHROPIC refs remain"</automated>
    <automated>cd /Users/salfaqih/paul/Projects/watchman && grep "\[ \] \*\*SLCK-0" .planning/REQUIREMENTS.md | wc -l | xargs test 0 -eq && echo "PASS: SLCK checked" || echo "FAIL: SLCK unchecked"</automated>
    <automated>cd /Users/salfaqih/paul/Projects/watchman && grep "\[ \] \*\*OUT-0" .planning/REQUIREMENTS.md | wc -l | xargs test 0 -eq && echo "PASS: OUT checked" || echo "FAIL: OUT unchecked"</automated>
    <automated>cd /Users/salfaqih/paul/Projects/watchman && python -m pytest tests/ -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Zero grep matches for datetime.utcnow() in src/ and tests/. Zero grep matches for ANTHROPIC_API_KEY in the three SUMMARY files. All SLCK-01-04 and OUT-01-03 checkboxes are checked. All existing tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create VERIFICATION.md files for Phase 1 and Phase 3</name>
  <files>
    .planning/phases/01-collection-pipeline/01-VERIFICATION.md
    .planning/phases/03-enrichment-pipeline/03-VERIFICATION.md
  </files>
  <action>
  Create verification reports following the format used by `.planning/phases/02-scoring-and-slack-review/02-VERIFICATION.md`. Each file has:

  **Frontmatter:** phase, verified (today's date), status, score, human_verification list

  **Body sections:**
  1. Goal Achievement with Observable Truths table (# | Truth | Status | Evidence)
  2. Must-Have Analysis
  3. Code Quality observations
  4. Known Issues

  **Phase 1: Collection Pipeline** (`01-VERIFICATION.md`):
  - Phase goal: "Signals flow from external sources into normalized, deduplicated cards in the database, with source health monitored"
  - Verify against Phase 1 success criteria from ROADMAP.md (5 criteria)
  - Read existing source code to gather evidence:
    - `src/watchman/sources/registry.py` and `config/sources.yaml` for SRC-01-03
    - `src/watchman/collectors/` for COLL-01-04
    - `src/watchman/processing/` for PROC-01-03
    - `src/watchman/storage/` for INFRA-01
    - `src/watchman/models/` for INFRA-02
  - Status likely `verified` or `human_needed` depending on what requires live env

  **Phase 3: Enrichment Pipeline** (`03-VERIFICATION.md`):
  - Phase goal: "Approved signals are automatically enriched with capabilities, pricing, API surface, and integration details, producing draft tool entries"
  - Verify against Phase 3 success criteria from ROADMAP.md (3 criteria)
  - Read existing source code to gather evidence:
    - `src/watchman/enrichment/` for enrichment logic
    - `src/watchman/slack/actions.py` or similar for approval trigger
    - `src/watchman/models/` for Pydantic schema validation
  - Status likely `human_needed` for live Slack approval verification

  Use `Read` and `Grep` tools to gather actual line references and evidence from the codebase. Do NOT fabricate evidence — verify each truth against actual code.
  </action>
  <verify>
    <automated>test -f .planning/phases/01-collection-pipeline/01-VERIFICATION.md && echo "PASS: Phase 1 verification exists" || echo "FAIL: Missing"</automated>
    <automated>test -f .planning/phases/03-enrichment-pipeline/03-VERIFICATION.md && echo "PASS: Phase 3 verification exists" || echo "FAIL: Missing"</automated>
    <automated>grep -c "Observable Truths" .planning/phases/01-collection-pipeline/01-VERIFICATION.md | xargs test 0 -lt && echo "PASS: Phase 1 has truths table" || echo "FAIL"</automated>
    <automated>grep -c "Observable Truths" .planning/phases/03-enrichment-pipeline/03-VERIFICATION.md | xargs test 0 -lt && echo "PASS: Phase 3 has truths table" || echo "FAIL"</automated>
  </verify>
  <done>01-VERIFICATION.md and 03-VERIFICATION.md exist with proper frontmatter, Observable Truths tables with evidence from actual code, and status fields.</done>
</task>

</tasks>

<verification>
Run all checks:
1. `grep -r "datetime.utcnow()" src/ tests/ --include="*.py"` returns zero results
2. `grep "ANTHROPIC_API_KEY" .planning/phases/02-scoring-and-slack-review/02-01-SUMMARY.md .planning/phases/03-enrichment-pipeline/03-0*-SUMMARY.md` returns zero results
3. `grep "\[ \] \*\*SLCK-0" .planning/REQUIREMENTS.md` returns zero results
4. `grep "\[ \] \*\*OUT-0" .planning/REQUIREMENTS.md` returns zero results
5. `test -f .planning/phases/01-collection-pipeline/01-VERIFICATION.md` succeeds
6. `test -f .planning/phases/03-enrichment-pipeline/03-VERIFICATION.md` succeeds
7. `python -m pytest tests/ -x -q` all tests pass
</verification>

<success_criteria>
- Zero deprecated datetime.utcnow() calls in source and test code
- SUMMARY docs accurately reference OPENROUTER_API_KEY
- REQUIREMENTS.md checkboxes match actual implementation status
- Phase 1 and Phase 3 have VERIFICATION.md files with evidence-based truths
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/06-tech-debt-and-doc-sync/06-01-SUMMARY.md`
</output>
